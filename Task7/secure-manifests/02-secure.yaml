apiVersion: v1
kind: Pod
metadata:
  name: pod-secure-volume
  namespace: audit-zone
spec:
  containers:
    - name: nginx
      image: nginxinc/nginx-unprivileged
      volumeMounts:
        - name: app-data
          mountPath: /var/data
        - name: nginx-cache
          mountPath: /var/cache/nginx
        - name: nginx-pid
          mountPath: /var/run
        - name: nginx-tmp
          mountPath: /tmp
      securityContext:
        allowPrivilegeEscalation: false
        capabilities:
          drop:
            - ALL
        readOnlyRootFilesystem: true
  volumes:
    - name: app-data
      emptyDir: { }
    - name: nginx-cache
      emptyDir: { }
    - name: nginx-pid
      emptyDir: { }
    - name: nginx-tmp
      emptyDir: { }
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
